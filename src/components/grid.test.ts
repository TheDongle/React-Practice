// import { describe, expect, it } from "@jest/globals";
// import Grid from "./grid";

// describe("grid", () => {
//   it("Should have a column", () => {
//     const grid = new Grid();
//     expect(grid).toHaveProperty("row");
//     expect(grid.row).toHaveLength(10);
//   });
//   it("Should have a row", () => {
//     const grid = new Grid();
//     expect(grid).toHaveProperty("column");
//     expect(grid.row).toHaveLength(10);
//   });
// });

